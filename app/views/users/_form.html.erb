<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <label class="label">Add your name to start recording your metrics</label>
  <div class="control">
    <input class="input" type="text" name="user[name]" placeholder="Name">
  </div>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
